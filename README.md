# Templatemengo!

Visit the [Templatemengo website](https://comp.quirino.net).

To build the website install `uv` and run the following:
```sh
uv run -m mkdocs build
```

To serve a local copy on localhost run:
```sh
uv run -m mkdocs serve
```
